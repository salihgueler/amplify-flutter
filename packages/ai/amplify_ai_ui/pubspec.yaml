name: amplify_ai_ui
description: Flutter UI components for Amplify AI Kit — drop-in chat widgets, streaming text, tool cards, and generation views.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  amplify_ai:
    path: ../amplify_ai

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
