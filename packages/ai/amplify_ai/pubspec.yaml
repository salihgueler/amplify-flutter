name: amplify_ai
description: Amplify AI Kit for Flutter. Uses Amplify.API directly — no extra configuration needed.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  amplify_flutter: '>=2.0.0'
  meta: any
  collection: any

dev_dependencies:
  test: ^1.24.0
